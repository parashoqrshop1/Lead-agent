# Streamlit Community Cloud — NO pydantic (Python 3.14 build breaks pydantic-core)
# Prefer selecting Python 3.12 in Streamlit Advanced settings if available.

streamlit>=1.40.0
pandas>=2.2.0
numpy>=1.26.0
python-dotenv>=1.0.0
httpx>=0.27.0
openpyxl>=3.1.0
tenacity>=8.2.0
pyyaml>=6.0.1
beautifulsoup4>=4.12.0

# Free Google Sheets
gspread>=6.0.0
google-auth>=2.28.0

# Free Gemini (used only in light scrape mode)
google-generativeai>=0.8.0
