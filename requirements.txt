# Streamlit Community Cloud — keep this LIGHT to avoid segfaults / OOM.
# Pin versions for stability on free hosts.

streamlit==1.41.1
pandas==2.2.3
numpy==1.26.4
pydantic==2.10.3
python-dotenv==1.0.1
httpx==0.28.1
openpyxl==3.1.5
tenacity==9.0.0
pyyaml==6.0.2
beautifulsoup4==4.12.3
# NOTE: do NOT install lxml on Streamlit Cloud (native segfault risk)

# Free Google Sheets (optional; safe pure-ish stack)
gspread==6.1.4
google-auth==2.37.0

# Free Gemini client (lazy-imported only when light scrape runs)
google-generativeai==0.8.3
