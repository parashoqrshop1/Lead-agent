# Optional FULL scraper stack (local PC, Codespaces, Railway, VPS)
# Install AFTER requirements.txt when you want open_source live scrape:
#   pip install -r requirements.txt -r requirements-scraper.txt
#   playwright install chromium

scrapegraphai>=1.40.0
playwright>=1.40.0
langchain-google-genai>=2.0.0
langchain-groq>=0.2.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
